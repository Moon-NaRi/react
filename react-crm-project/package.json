{
    "name": "react-crm-project",
    "version": "1.0",
    "scripts": {
        "client": "cd client && npm start",
        "server": "nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
    },
    "dependencies": {
        "body-parser": "^1.20.2",
        "ejs": "^3.1.10",
        "express": "^4.19.2",
        "http": "^0.0.1-security",
        "http-proxy-middleware": "^3.0.0",
        "mariadb": "^3.3.0"
    },
    "devDependencies": {
        "concurrently": "8.2.2"
    }
}
